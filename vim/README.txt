                                      vim

                                      ---

                            Example Root: /home/user

This module contains a very stripped down 'vim' configuration, plus a
colortemplate. Note that the colortemplate does nothing by itself, but it can be
used to regenerate the color scheme in the '.vim/doc' directory and its
documentation '.vim/doc' if need be.
