                                      nnn

                                      ---

As 'nnn' is configured via environment variables, which are set in the files of
the 'shell' module and not here, this module contains only a plugin for 'nnn'
which makes use of 'jdfs' — if that is installed — to jump around in the
filesystem.
